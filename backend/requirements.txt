# --- Framework web ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Conversión de Office y texto (Microsoft MarkItDown) ---
# Solo extras de Office: los PDFs se procesan con PyMuPDF, y las imágenes
# y PDFs escaneados se delegan al LLM del cliente (sin OCR en el servidor).
markitdown[docx,xlsx,pptx]>=0.1.1

# --- Extracción rápida de texto nativo de PDFs (100% en memoria) ---
PyMuPDF>=1.24.0
